{
  "schema_version": "capability_mesh.schema_manifest.v1",
  "schemas": [
    {
      "schema_id": "setup_editor.sandbox.request.v1",
      "description": "Sandbox echo request payload.",
      "fields": [
        {
          "name": "message",
          "type": "string",
          "description": "Message echoed by the sandbox route when a real binding existed."
        }
      ]
    },
    {
      "schema_id": "setup_editor.sandbox.response.v1",
      "description": "Sandbox echo response payload.",
      "fields": [
        {
          "name": "message",
          "type": "string",
          "description": "Echoed message."
        }
      ]
    }
  ]
}
